## Plugin Configuration
---
package:
  author: KBase Developer
  name: job-browser2
  description: The job browser panel and associated widgets
  type: legacy
install:
  route:
    routes:
      - path: "jobbrowser?:-tab"
        authorization: true
        view: browse
  menu:
    - name: jobbrowser
      definition:
        path: ["jobbrowser"]
        label: Jobs
        icon: suitcase
